<navigation:ExamplePage xmlns:navigation="navigation/example-page" loaded="onPageLoaded" xmlns:lv="nativescript-pro-ui/listview" xmlns="http://www.nativescript.org/tns.xsd">
    <GridLayout rows="auto, *">
        <StackLayout orientation="horizontal">
            <Button text="{{isFilteringEnabled ? 'Disable filtering' : 'Enable filtering'}}" tap="toggleFilter" margin="5"/>
            <Button text="{{isSortingEnabled ? 'Disable sorting' : 'Enable sorting'}}" tap="toggleSorting" margin="5"/>
            <Button text="{{isGroupingEnabled ? 'Disable grouping' : 'Enable grouping'}}" tap="toggleGrouping" margin="5"/>
        </StackLayout>
        <lv:RadListView id="myListView" row="1" items="{{ dataItems }}" 
                        sortingFunction="{{ mySortingFunc }}"
                        filteringFunction="{{ myFilteringFunc }}"
                        groupingFunction="{{ myGroupingFunc }}">
            <lv:RadListView.itemTemplate>
                <StackLayout orientation="vertical">
                    <Label fontSize="20" text="{{ itemName }}"/>
                    <Label fontSize="14" text="{{ itemDescription }}"/>
                </StackLayout>
            </lv:RadListView.itemTemplate>
            <lv:RadListView.headerItemTemplate>
                <GridLayout backgroundColor="#65a565">
                    <Label text="{{ category }}"/>
                </GridLayout>
            </lv:RadListView.headerItemTemplate>
        </lv:RadListView>
    </GridLayout>
</navigation:ExamplePage>
